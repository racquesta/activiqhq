import { notImplementedResponse } from "@/app/api/_not-implemented";

/**
 * Child profile creation (duplicate detection in later task).
 */
export async function POST() {
  return notImplementedResponse("POST /api/o/[orgSlug]/children");
}
